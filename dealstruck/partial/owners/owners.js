angular.module('dealstruck').controller('OwnersCtrl', ['$scope', 'userService', 'companyService',
	function($scope, userService, companyService) {

		// Not need yet - handling all scope on the dealsCtrl for now to avoid code duplication

	}
]);